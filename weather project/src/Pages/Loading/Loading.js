import React from "react";
import "./Loading.scss";
const Loading = () => {
  return (
    <section className="loading">
      <h2 class="animate">Loading</h2>
    </section>
  );
};

export default Loading;
